rootProject.name = "kspigot-plugin-template"

pluginManagement {
    repositories {
        gradlePluginPortal()
        // PaperMC Repo
        maven("https://repo.papermc.io/repository/maven-public/")
    }
}